SELECT
    CONCAT(GetMetadataPropertyValue([aks-events], '[User].[cosmic_cluster_id]'), '.', [event].[metadata].[name]) AS id,
    *
INTO [cosmic-events-aks-db-plat]
FROM [aks-events]
WHERE [event].[type] = 'Warning' AND [event].[reason] != 'DeletionObserved'